# GPU exploration

- `gpumode/` — PyTorch-extension kernels (use `utils.load_cuda`)
- `pmpp/` — standalone CUDA C programs (use `./compile.sh`)

Edit locally on Mac, push, then pull + run in Lightning AI Studio.
